{
  "name": "@genoffice/platform",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "description": "Capability-scoped platform port interfaces (language / ai + aiSettings + aiChat + genspark / search / attachments / project / window) with a runtime-agnostic resolver; no Electron dependency",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@genoffice/ai-provider": "*",
    "@genoffice/i18n": "*",
    "@genoffice/project-store": "*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
