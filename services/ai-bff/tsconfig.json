{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Server-side only: no DOM lib, which is also a structural guarantee that
    // nothing browser-shaped leaks into the credential-holding process.
    "lib": ["ES2022"],
    "types": ["node"]
  },
  "include": ["src", "tests", "vitest.config.ts"]
}
